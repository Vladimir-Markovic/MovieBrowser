<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/llFragmentMain"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainFragment"
    tools:showIn="@layout/activity_main"
    >

    <Spinner
        android:id="@+id/spnSort"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorGray"
        android:paddingBottom="@dimen/spinner_ver_padding"
        android:paddingLeft="@dimen/spinner_hor_padding"
        android:paddingStart="@dimen/spinner_hor_padding"
        android:paddingEnd="@dimen/spinner_hor_padding"
        android:paddingRight="@dimen/spinner_hor_padding"
        android:paddingTop="@dimen/spinner_ver_padding"
        />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rvPosters"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="false"
        android:background="@color/colorLight"
        android:padding="@dimen/half_poster_spacing"
        android:scrollbarThumbHorizontal="@drawable/scrollbar"
        android:scrollbars="vertical"
        />

</LinearLayout>
